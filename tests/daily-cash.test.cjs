const {test}=require('node:test'),assert=require('node:assert/strict');
const {project}=require('../packages/engine/dist');
const {explainBalance}=require('../apps/api/dist/explanation');
test('salário e despesas na mesma data resultam em menor caixa diário correto',()=>{const s={asOf:'2026-09-10',syncedAt:new Date().toISOString(),accounts:[{id:'a',name:'Conta',balanceCents:1685}],commitments:[{id:'b',name:'Conta A',amountCents:84200,dueDate:'2026-09-20',status:'pending'},{id:'c',name:'Conta B',amountCents:87031,dueDate:'2026-09-20',status:'pending'},{id:'d',name:'Conta C',amountCents:20207,dueDate:'2026-09-23',status:'pending'}],incomes:[{id:'i',name:'Extra',date:'2026-09-11',amountCents:10000,confirmed:true}],salary:{netCents:110000,nextDate:'2026-09-20',payday:20,confirmed:true,accountId:'a'},transactions:[],goals:[],bufferCents:0};const p=project(s);assert.equal(p.timeline.find(d=>d.date==='2026-09-20').balanceCents,-49546);assert.equal(p.minCashCents,-69753);assert.equal(p.minDate,'2026-09-23');assert.match(explainBalance(s).reply,/1.200,00/);s.salary.payday=21;assert.equal(project(s).minCashCents,-159546);});
